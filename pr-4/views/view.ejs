<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Book Store</title>

    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
    <script src="https://code.jquery.com/jquery-3.3.1.slim.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.7/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js"></script>

</head>
<style>
    body {
        height: 100vh;
        margin: 0;
        background:#000000e3;
    }

    .book {
        background-color: #d1e9e9;
        font-family: Arial, sans-serif;
        padding: 20px 15px;
        width: 290px;
        border-radius: 10px;
        display: flex;
        justify-content: center;
    }

    .book-cover {
        background-color: #1a2b44;
        color: #fff;
        width: 250px;
        height: 340px;
        border-radius: 10px;
        padding: 20px;
        box-shadow: 0px 4px 20px rgba(0, 0, 0, 0.2);
        display: flex;
        flex-direction: column;
    }

    .book-title {
        font-size: 24px;
        font-weight: bold;
        text-align: center;
        margin: 0;
    }

    .author {
        font-size: 18px;
        text-align: center;
        margin-top: 10px;
        font-style: italic;
        color: #a0c4ff;
    }

    .details {
        margin-top: 20px;
        text-align: center;
    }

    .details p.pages {
        margin-bottom: 30px;
    }

    .price,
    .pages {
        font-size: 16px;
        margin: 5px 0;
        color: #fbb13c;
    }

    .action {
        margin-top: 40px;
    }

    .del {
        margin-right: 20px;
    }

    .add{
        margin: 20px 0px 0px 70px ;
    }
</style>

<body>
    <div class="container-fluid m-5 d-flex">

        <% record.map((val,i)=>{
            const {id,name,author,price,pages} = val;
            %>

            <div class="book m-2">

                <div class="book-cover">
                    <h1 class="book-title">
                        <%= name %>
                    </h1>
                    <p class="author">
                        <%= author %>
                    </p>
                    <div class="details">
                        <p class="price">Price: $╣<%= price %>
                        </p>
                        <p class="pages">Number of Pages: <%= pages %>
                        </p>
                    </div>
                    <div class="action">
                        <a href="/delEntry?delId=<%= id %>" class="del btn btn-danger">DELETE</a>
                        <a href="/editBook?editId=<%= id %>" class="btn btn-info">UPDATE</a>
                    </div>
                </div>
            </div>

            <% }) %>

        </div>
        <a href="/" class="btn btn-warning add">Add</a>
</body>

</html>